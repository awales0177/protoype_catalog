{
  "name": "uux",
  "private": true,
  "workspaces": [
    "catalog"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "npm run dev -w catalog_ds",
    "start": "npm run dev -w catalog_ds",
    "dev:catalog-ds": "npm run dev -w catalog_ds",
    "dev:all": "turbo run dev --parallel",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.4.4"
  },
  "overrides": {
    "nth-check": "^2.1.1",
    "postcss": "^8.5.6",
    "eslint": {
      "ajv": "^6.12.6"
    },
    "@eslint/eslintrc": {
      "ajv": "^6.12.6"
    }
  }
}
